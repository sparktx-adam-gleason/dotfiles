#!/usr/bin/env bash

export PATH="/usr/local/bin:/usr/local/sbin:~/$PYENV/bin:$PATH"

# Virtualenvs
eval "$(/opt/homebrew/bin/brew shellenv)"
eval "$(pyenv init -)"
if which pyenv-virtualenv-init > /dev/null; then eval "$(pyenv virtualenv-init -)"; fi
eval "$(direnv hook zsh)"

